<main class="home-page">
  <p>👋 welcome to jottr! <a href="/" class="create-new">create new jot</a></p>

  <% unless @jots.count.zero? %>
    <div>
      <p>old jots</p>

      <% @jots.each do |jot| %>
        <p><a href="/jots/<%= jot[:id] %>"><%= jot[:title] || jot[:id] %></a></p>
      <% end %>
    </div>
  <% end %>
</main>
